# TV-Minder

Full-stack application to track TV series

## Front-end

React, chakra ui

## API

### Build Docker Container

`-p` flag maps the ports (local:docker)

```terminal
docker run -d -p 5000:5000 tv-minder-api
```