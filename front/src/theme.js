import { theme as defaultTheme } from '@chakra-ui/core';

export default {
  ...defaultTheme,
  colors: {
    ...defaultTheme.colors,
    black: '#333333',
    primary: '#0099DB',
    secondary: '#034A85',
  },
};
